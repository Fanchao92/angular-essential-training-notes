import { Component } from '@angular/core';

@Component({
	selector: 'mw-app',
	templateUrl: 'app/app.component.html', // NOTE: A path relative to the root of the project
	styleUrls: ['app/app.component.css'] // NOTE: The urls for style sheets
}) // NOTE: Selector and Template are must-have in the Component metadata
export class AppComponent {
	;
}